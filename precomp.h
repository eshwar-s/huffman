#include <list>
#include <queue>
#include <stack>
#include <cstdio>
#include <string.h>
#include <string>
#include <queue>
#include <memory>
#include <sstream>
#include <map>

#define abs(x) ((x) < 0 ? -(x) : (x))

int gcd(int a, int b);
int max(int a, int b);
void swap(int* i, int* j);
void swap(char* arr, int i, int j);
void print_array(int arr[], int n);
void print_2d_array(int* arr, int row, int col);
void print_string(const char* str, int low, int high);